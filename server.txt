python -m http.server 8080

http://localhost:8080

Firebase project console:
https://console.firebase.google.com/project/the-librarian-ff14b/overview

Firebase Hosting URL:
https://the-librarian-ff14b.web.app

Firestore (telemetry):
- Create a Cloud Firestore database in the Firebase console (if not already).
- Deploy rules: firebase deploy --only firestore:rules
- Events collection: "events" (written automatically when a user is signed in).